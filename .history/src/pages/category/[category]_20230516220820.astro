---
import { slugify } from "../../js/utils";

export async function getStaticPaths() {
  return [
    {
      params: { category: slugify("R"},
      props: {},
    }
  ]
}


const {??} = Astro.params;
const {??} = Astro.props;
---