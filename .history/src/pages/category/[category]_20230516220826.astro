---
import { slugify } from "../../js/utils";

export async function getStaticPaths() {
  return [
    {
      params: { category: slugify("Referece D"},
      props: {},
    }
  ]
}


const {??} = Astro.params;
const {??} = Astro.props;
---