---
import { slugify } from "../../js/utils";

export async function getStaticPaths() {
  return [
    {
      params: { category: slugify("Refe"},
      props: {},
    }
  ]
}


const {??} = Astro.params;
const {??} = Astro.props;
---