---
import { slugify } from "../../js/utils";

export async function getStaticPaths() {
  return [
    {
      params: { category: slugify("Re"},
      props: {},
    }
  ]
}


const {??} = Astro.params;
const {??} = Astro.props;
---