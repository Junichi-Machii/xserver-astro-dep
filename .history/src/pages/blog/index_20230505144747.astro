---

// conponent imports
import MainLayout from "../../layouts/MainLayout.astro";
import PostCard from "../../compone/PostCard.astro";

const allPosts = await Astro.glob("./*.md");
---

<MainLayout title="home">
  {
    allPosts.map((post) => <PostCard
    {post} />)
  }
</MainLayout>